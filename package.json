{
  "name": "ansible-playbooks",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "format": "prettier --ignore-unknown --write .",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
